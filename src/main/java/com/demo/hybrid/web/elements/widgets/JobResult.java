package com.demo.hybrid.web.elements.widgets;

import com.demo.hybrid.web.elements.Element;
import com.demo.hybrid.web.utils.WebAutomationContext;
import org.openqa.selenium.WebElement;

public class JobResult extends Element {

    public JobResult(WebAutomationContext context, WebElement toWrap) {
        super(context, toWrap);
    }
}
